{
  "name": "stripe-connect-sample",
  "version": "1.0.0",
  "description": "Comprehensive Stripe Connect integration sample with detailed code comments",
  "main": "dist/server.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "test": "tsc --pretty false --outDir dist --rootDir . --module ES2020 --moduleResolution node --target ES2020 --strict --esModuleInterop --skipLibCheck checkout-redirects.ts checkout-redirects.test.ts && node --test dist/checkout-redirects.test.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "stripe": "^15.0.0",
    "body-parser": "^1.20.2",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "typescript": "^5.0.0",
    "ts-node": "^10.9.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
